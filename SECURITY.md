# 安全策略

## 支持版本

| 版本 | 支持 |
| --- | --- |
| 0.1.x | ✅（仅最新 minor） |
| 更早 | ❌ |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。** 通过 GitHub Security Advisories
（"Report a vulnerability"）私下报告，或联系仓库维护者。收到报告后：

- 72 小时内确认收到；
- 7 天内给出初步评估（影响面 / 利用条件 / 严重度）；
- 修复随下一个 patch 版本发布，并在 CHANGELOG 中致谢报告者（除非要求匿名）。

## 安全设计要点（v1 现状，单机离线部署）

- **认证**：三员分岗（系统管理员 / 安全保密管理员 / 安全审计员，GB/T 28452），
  SM2 挑战-应答登录（密钥不出盘明文），账户锁定，一次性 IPC 令牌。
- **加密**：数据库与图像静态加密 SM4-CBC + HMAC-SM3（历史 AES-256-GCM 兼容
  解密）；报告签名 SM2（SM3withSM2）；审计日志 append-only 双哈希链
  （新记录 SM3，历史 SHA-256，`verify_chain` 双算法兼容）。
- **网络**：后端仅绑定 127.0.0.1；前端 CSP 严格 `default-src 'self'`，connect/img
  仅限本机端口；出网守卫默认阻断；依赖安装走构建脚本白名单
  （pnpm `onlyBuiltDependencies`）。
- **进程**：Tauri 壳监督 sidecar 生命周期（崩溃自愈、父进程消失自杀、孤儿回收）；
  数据目录与安装目录分离（卸载不丢业务数据）。

## 已知限制（诚实声明）

1. **安装包未做代码签名**（暂无代码签名证书）：SmartScreen / 杀软可能提示
   "未知发布者"。校验方式：比对 Release 页附带的安装包哈希（sha256sum）。
2. **无自动更新**：升级为手动换包（安装程序内含旧数据迁移）。
3. 单机设计：操作员身份以本机三员账号为准，无集中身份源；审计链防篡改
   但未引入外部可信时间戳（RFC 3161 为演进项）。
4. `data/bootstrap_keys/` 的三员引导密钥适用于开发/演示；**正式交付必须
   用部署基线文档的引导流程重新生成**，切勿复用示例密钥。
