# Node / Frontend
node_modules/
dist/
*.local

# Python
__pycache__/
*.py[cod]
.venv/
backend/.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.pyright/
.coverage
.coverage.*

# Data & artifacts（DVC 追踪的原始数据除外）
data/tmp/
data/.dvc/tmp/
data/scan.db
data/scan.db-shm
data/scan.db-wal
data/images/
data/reports/
data/real_label/
data/llm/
data/training/
data/external/
data/compare/
data/runs/
data/batch/
# 真实底片衍生产物不入库（权重与真实工业数据均不入库）
data/atlas/
data/rejects/
data/demo/
data/prelabels/
backend/data/
data/eval/.gate_work/
data/eval/evidence/
# 部署后评估的时间戳历史快照（每次运行落一份，只入库非时间戳的最新报告）
data/eval/deployed_eval_*_2[0-9][0-9][0-9][0-9][0-9][0-9][0-9]_[0-9][0-9][0-9][0-9][0-9][0-9].json
data/experiments/
runs/
backend/models/weights/*.onnx
backend/models/weights/*.pt

# 打包/构建产物（非源码，不入库）
_pkg/
installer_src/
# 嵌入版 Python + 依赖（Electron 打包运行时，体积大，不入库）
src/python_embed/
# 本地模型副本（安装包由 backend/models/weights 注入，不入库）
models/weights/
# 本地大模型运行时与权重（llama.cpp 二进制 + GGUF，随安装包分发，不入库）
tools/llama/
models/llm/
nul

# 本地构建/调试产物（不入库）：构建日志、下载的二进制包、
# 本地 cargo 镜像配置（rsproxy，个人网络环境，不入库防 CI 失败）
.cargo/
build_log*.txt
cli.tgz

# Env / secrets
.env
.env.*
!.env.example

# 本地工具的工作区缓存
.workbuddy/

# OS
.DS_Store
Thumbs.db

# Vite 构建崩溃临时产物
src/vite.build.config.ts.timestamp-*.mjs
src/vite.config.ts.timestamp-*.mjs

# 运行时密钥/吊销状态/内存（绝不入库）
.dsh-memory/
data/.auth_secret
data/.crypto_key
data/ipc_token
data/bootstrap_keys/
data/.revoked_tokens.json

# 软著材料与运行时产物（本地生成，不入库）
output/
.build_installer.log

# 私有资料（绝不入库）：用户自有底片/照片、竞赛文档、标准原文（受版权保护，
# 不得转载分发）、本地运行时产物
图片/
定检/
射线竞赛/
承压设备射线检测缺陷自动识别系统评价方法.pdf
measure_signals.json
train_status.json
data/m6_demo_report.pdf
data/eval/std_eval.json
data/eval/std_record.json
data/eval/std_personnel.json
data/*.db.bak
data/compliance/

# 运行时数据与会话产物不入库（重装前快照 2026-08-30）
data/backups/
data/privacy/
data/rejects/
_doc_task/
# 本地要求类文档：不入库
/*.docx

# 本地工具缓存
.mimosa/
.dsh-memory/
.import_linter_cache/
data/sync/
data/active/
data/model_registry.json
data/eval/.gate_work/


# === 公开仓安全边界 ===
# 无关工具目录与脚本
.trae/
_zip_inspect/
**/crawl_judgments.py
# 密钥与证书
*.pem
*.key
!**/*.example

# 本地大模型评片端到端验证的一次性数据目录（scripts/verify_local_llm_report.py）
.verify-run-*/
# Electron 打包产物（electron-builder 输出，安装包为交付物不入库）
src/release/
# 后端开发态日志（SCANDETECTION_USER_DATA_DIR=仓库根时落在 /logs）
/logs/
