# 射线焊缝缺陷智能检测系统 — 技术规格书（专业级 / 抗过时 v0.2）

> 版本：v0.2（专业级草案，面向三年生命周期）
> 日期：2026-08-04
> 定位：工程实现的**工程级规格书**。设计目标是：架构与关键技术范式在 2026–2029 周期内不陈旧；功能达到工业无损检测（NDT）专业水准；可通过插件机制适配标准修订与新模型。
> 标准声明：NB/T47013.2-2015 具体数值表来自公开解读，**非官方原文**，以 `{{STD_TABLE_*}}` 占位，正式实现须以授权标准文本复核替换。

## 使用说明
- **本规格书是唯一事实源**：任何一轮开发任务必须以本文档的接口契约（§19.3）、API 契约（§14）、编程规范（§13）为准，禁止自行发明架构或绕过接口。
- **增量开发按 §19.5 任务模板开工，按 §19.9 清单合并**；CI（§19.6）机械防漂移，不靠人工记忆。
- **架构扩展只允许"接口不动、实现可插拔"（§19.4）**，这是从根源遏制技术债务的根本纪律。
- 分层依赖铁律（§19.1）由 `import-linter` 强制，违反即 CI 红灯，杜绝层间越权。

---

## 0. 设计原则（决定"三年不过时"的底层约束）

1. **接口驱动、实现可插拔**：所有算法/模型/标准均为接口背后的可替换实现（依赖倒置）。三年后 YOLO 被新检测器取代、NB/T47013 出新版，仅需新增适配，不改动主干。
2. **拥抱开放标准**：影像用 DICOM（DR/CR 工业设备原生输出）、模型用 ONNX 作为交换格式、报告用 PDF/A 归档、存储用 SQL（SQLite 本地↔PostgreSQL 云端）。避免私有格式锁定。
3. **本地优先、隐私优先**：工业底片属敏感数据，v1 全离线；云能力以"可选同步适配器"存在，绝不默认上云。
4. **数据centric + 闭环进化**：大量未标注数据的价值通过半监督/主动学习释放；人工复核结果回流驱动模型迭代（持续学习闭环）。
5. **可观测、可复现**：实验可复现（固定种子、数据版本化）、运行可观测（日志/指标）、决策可解释（判定溯源到标准条款与计算值）。

---

## 1. 系统定位与演进范围

### 1.1 产品
桌面端智能评片系统：输入焊缝射线影像（**DICOM / CR / DR / 通用图像**），自动完成底片质量校验 → 预处理 → 缺陷检测与量化 → 标准判定 → 报告，全流程本地闭环。

### 1.2 演进路线（分层交付，避免一次性过度设计）
| 阶段 | 范围 | 关键能力 |
|---|---|---|
| **v1（本次实现）** | 纯桌面，端+边本地 | 全套算法 + 本地 SQLite + 单机评片 |
| v2 | 边缘集群 | 多设备并联、边缘节点协同、本地模型服务化 |
| v3 | 云端 + 联邦学习 | 跨站点数据不出行的前提下联邦模型进化、远程管理与大数据分析 |

v1 在架构层**预留** v2/v3 接口（见 §3.2、§7.6），但不实现其基础设施。

### 1.3 五类目标缺陷（同 v0.1，作为检测类别基线）
气孔 / 夹渣 / 未焊透 / 未熔合 / 裂纹；标准判定时再按长宽比归为圆形（L/W≤3）或条形（>3）缺陷。

---

## 2. 非功能性需求（NFR，可量化验收）

| 类别 | 指标 | 目标值 |
|---|---|---|
| 性能 | 单张预处理（CPU） | ≤ 1.0 s；GPU ≤ 200 ms |
| 性能 | 缺陷检测推理（GPU） | ≤ 150 ms/图；CPU ≤ 500 ms |
| 精度 | 检测 mAP@0.5（验证集） | ≥ 0.90 |
| 精度 | 缺陷尺寸量化相对误差 | ≤ 5%（亚像素+标定） |
| 精度 | 评级与高级评片员一致率 | ≥ 95%（不含推送人工的疑难样本） |
| 鲁棒性 | 跨设备/跨工况（不同机位、黑度） | 一致率下降 ≤ 5%（需校准） |
| 合规 | 离线运行、无外网依赖 | 强制 |
| 安全 | 本地数据加密存储、访问控制 | 强制 |
| 可维护 | 模型/标准/算法均可热插拔 | 接口契约锁定 |
| 可移植 | Windows 为主，兼容 Linux 构建 | 打包双平台 |

---

## 3. 总体架构

### 3.1 分层架构（DDD 风格，接口隔离）
```
┌──────────────────────────────────────────────────────────┐
│ 表现层 (Presentation)  Tauri + Vue3/TS                      │
│   底片导入(DICOM/CR/DR) · 预览 · 结果/报告 · 人工复核 UI     │
└───────────────────────────┬──────────────────────────────┘
                            │  REST / gRPC (127.0.0.1, 带 token)
┌───────────────────────────▼──────────────────────────────┐
│ 应用层 (Application)     FastAPI 用例编排                  │
│   管线编排: 质量校验→预处理→检测→量化→判定→报告            │
└───────────────────────────┬──────────────────────────────┘
                            │  领域接口(Protocol/ABC)
┌───────────────────────────▼──────────────────────────────┐
│ 领域层 (Domain)        算法与规则（可替换实现）             │
│  IQIVerifier · Preprocessor · DefectDetector · Quantifier │
│  · StandardGrader(多标准适配) · Reporter · Syncer(预留)    │
└───────────────────────────┬──────────────────────────────┘
                            │  基础设施接口
┌───────────────────────────▼──────────────────────────────┐
│ 基础设施层 (Infrastructure)                                │
│  ModelStore(ONNX/pt) · DB(SQLite/PG) · FS · Crypto · Log  │
└──────────────────────────────────────────────────────────┘
```
- **关键**：领域层只依赖接口（Protocol），具体实现（YOLO 哪一代、哪个标准版本）注入到基础设施层。扩展实现时先实现接口契约与默认实现，后续扩展零改动主干。

### 3.2 端边云演进架构
- **端（采集+前置）**：DICOM 接入、黑度/标定、轻预处理。
- **边（识别+判定）**：检测、量化、标准判定、报告（v1 全部本地）。
- **云（v3）**：`Syncer` 适配器抽象 `push()/pull()/federate()`；v1 实现 `LocalAdapter`（空操作），v3 换 `CloudAdapter`（PostgreSQL + 对象存储 + 联邦平均）。
- 设计为**数据不出行**：即使上云，也优先联邦学习而非原始图像汇聚。

### 3.3 进程与 IPC（已确认技术栈）
- Tauri 2.x 将 Python 后端（PyInstaller 打包为单 exe）声明为 `externalBin` sidecar，主进程拉起并管理生命周期。
- 通信：`http://127.0.0.1:<PORT>` REST（JSON），高吞吐场景可切 gRPC；一次性启动 token 防误调。
- 模型**启动时加载一次**常驻显存/内存；支持热加载新权重（模型版本切换）。

### 3.4 技术栈与"抗过时"说明
| 层 | 选型 | 抗过时理由 |
|---|---|---|
| 桌面壳 | Tauri 2.x + Vue 3 + TS + Vite | Rust 内核、体积小；Vue3 长期支持；前端与算法解耦 |
| 后端 | Python 3.12（兼容 3.13） | 科学计算生态 2029 仍主导；接口隔离使语言可换 |
| 推理 | PyTorch 2.x → ONNX Runtime / TensorRT / OpenVINO | ONNX 为稳定交换格式，推理后端可随硬件演进 |
| 检测 | **模型无关接口**；默认 YOLOv8/v11(n)，可换 RT-DETR/DETR 系 | 检测器换代不伤主干 |
| 影像 | pydicom（DICOM）、OpenCV、scikit-image、NumPy | 开放标准/稳定库 |
| 存储 | SQLAlchemy（SQLite 本地 / PostgreSQL 云） | ORM 屏蔽数据库差异 |
| 报告 | WeasyPrint（HTML→PDF/A） | PDF/A 长期归档标准 |
| 标注 | Label Studio（独立） | 业界标准标注栈 |

### 3.5 强制采用的开放标准
- **DICOM**（ISO 12052）：DR/CR 原生格式，必须支持 `pydicom` 解析与窗宽窗位/灰阶还原。
- **ONNX**：模型训练→推理的解耦交换格式。
- **PDF/A**（ISO 19005）：报告长期归档，防格式失效。
- **SQL + 迁移脚本**：数据可迁移至云端。

---

## 4. 模块 2 — 影像接入、质量校验与预处理

### 4.1 影像接入（提升：支持工业原生格式）
- 支持 DICOM（含多帧、MONOCHROME1/2）、CR/DR 厂商私有转 DICOM、通用 PNG/JPG/TIFF。
- 还原 12–16bit 灰阶与真实黑度映射；记录 `pixel_spacing_mm`（来自 DICOM 标签或标定录入）。

### 4.2 底片质量校验（IQI 自动灵敏度验证）【专业新增】
- 自动定位**像质计（IQI）**：线型（wire）或孔型（hole），用模板匹配/小目标检测识别。
- 读取可达最细丝号 → 计算**影像灵敏度**，对照标准要求的 IQI 等级（A/AB/B 级对应不同灵敏度）判定底片是否"合格可评"。
- 同时校验：黑度范围（AB 级 2.0–4.5）、伪缺陷筛查。**不通过则阻断评片并提示重拍**——这是工业评片的硬前置。

### 4.3 预处理算法方案（升级）
1. **保边去噪**：默认 `bilateralFilter` + 轻度 `medianBlur`；颗粒噪声用低 `h` 非局部均值（`denoise_nl_means`）。
2. **对比度/灰度校正**：`Gamma`（低黑度补偿）+ `CLAHE`（局部不均）；可选 **Retinex(MSRCR)** 用于强不均底片。
3. **边缘增强**：非锐化掩膜（USM）细化焊缝与缺陷边缘（与去噪顺序错开，避免放大噪）。
4. **Canny 优化**：Otsu/局部自适应高低阈值 + 焊缝 ROI 限定。
5. **形态学**：开运算去孤立点、闭运算补断裂；核尺寸参数化。
6. **可选深度学习去噪路径**（heavy）：DnCNN/CBDNet 类网络作为"高精度模式"开关，默认关闭以保轻量。

### 4.4 质量度量（客观验收）
输出 PSNR/SSIM（有参考时）、**BRISQUE**（无参考盲评质量）作为预处理质量门禁，记录到日志。

### 4.5 接口与函数
- `POST /api/verify` → IQI 灵敏度 + 黑度 + 可否评片
- `POST /api/preprocess` → 增强图 + 质量指标
- 领域接口：`Preprocessor.denoise()`, `.enhance()`, `.edges()`, `.morph()`（各自可单测）

---

## 5. 模块 3 — 缺陷检测与量化（模型无关 + 精修 + 闭环）

### 5.1 检测接口（核心抽象，抗过时）
```python
class DefectDetector(Protocol):
    def load(self, model_uri: str, backend: str = "onnx") -> None: ...
    def infer(self, image, conf: float, iou: float) -> list[Detection]: ...
@dataclass
class Detection:
    bbox: tuple[float,float,float,float]   # x,y,w,h (px)
    mask: Optional[np.ndarray]             # 实例分割可选
    class_id: int
    score: float
    uncertainty: float                     # 不确定性估计
```
- 默认实现 `YOLODetector`（Ultralytics，导出 ONNX 由 ONNX Runtime 推理）；可加 `RTDETRDetector` / `DETRDetector` 不改主干。

### 5.2 网络结构约束（同 v0.1 并强化）
小卷积核堆叠 + **BiFPN/PAFPN 多尺度融合**（增强细缺陷）+ 自适应平均池化 + Dropout；训练分辨率 640，推理可降级提速。

### 5.3 实例分割精修（提升量化精度）【专业新增】
- 对检出框调用 **SAM2 / YOLOv8-seg** 生成精确掩膜 → `findContours` 得亚像素轮廓。
- 圆形/条形判定、面积、周长均由掩膜计算，比包围盒更准（尤其不规则夹渣、裂纹分支）。

### 5.4 量化与定位（亚像素 + 标定）
- 像素→物理：由 `pixel_spacing_mm` 标定；支持**标定块/标尺**自动识别校正。
- 几何属性：L、W（MinAreaRect）、面积、周长、**当量尺寸**（按标准定义）；位置坐标相对焊缝。
- 形态归类：aspect_ratio = L/W；≤3 圆形，>3 条形。

### 5.5 不确定性 + 主动学习闭环【抗过时关键】
- 推理期 **MC Dropout / 集成** 估计不确定性；高不确定性或近边界缺陷**自动推送人工复核**。
- 人工复核结果写入训练池 → **半监督/伪标签**或 **Mean Teacher** 迭代训练 → 模型版本升级。
- 形成"检测→人工→回流→再训练"的持续学习闭环（数据centric）。

### 5.6 数据策略（应对"大量未标注"）
- 已标注：8:1:1 划分（固定种子）。
- 未标注：Label Studio 标注 + **主动学习**挑高价值样本优先标。
- 增强：Albumentations（旋转/亮度/噪声/Mosaic）。
- 稀缺类：物理仿真生成缺陷样本（射线衰减模型或受控 GAN/扩散）扩充，提升细缺陷召回。

### 5.7 接口
- `POST /api/detect` → `Detection` 列表（含 uncertainty、mask 引用）+ 标注图
- 训练：`backend/models/train.py`（DVC 管理数据集版本，MLflow 记录实验）

---

## 6. 模块 4 — 标准智能判定（多标准适配 + 可解释 + 版本化）

### 6.1 标准适配层（抗过时核心）【专业新增】
```python
class StandardGrader(Protocol):
    standard_id: str          # "NB/T47013.2-2015" / "GB/T3323-2019" / "ASME-V" / "ISO5817"
    def grade(self, defects, context) -> GradeResult: ...
```
- 每种标准 = 一个实现模块，含其**规则 + 数值表（结构化 YAML/JSON）**。
- `standard_version` 写入每条记录，标准修订只需新增版本模块并切换，不影响历史数据可比性。
- v1 默认实现 **NB/T47013.2-2015**；预留 GB/T 3323、ASME BPVC Sec.V Art.2、ISO 17636/5817 适配器骨架。

### 6.2 NB/T47013.2-2015 判定框架（公开框架，待官方复核）
- **零容忍**：裂纹、未熔合、未焊透在 I/II/III 级均不允许 → 存在即 IV 级。
- **圆形缺陷**：按母材厚度 T 取评定区（T≤25→10×10；25<T≤100→10×20；T>100→10×30 mm）；点数查 `{{STD_TABLE_点数}}` 求和；长径<T/2 直判 IV；不计点数阈值 `{{STD_TABLE_不计}}`；I 级及 T≤5mm 的 II 级评定区内不计点缺陷>10 个降一级。
- **条形缺陷**：I 级不允许；II 级单条 ≤T/3(最小4)且≤20，组内(12T区)累计 ≤2T/3(最小6)且≤30；III 级放宽至 `{{STD_TABLE_条形}}`；同线间距≤小缺陷长度合并。
- **综合评级**：圆+条共存 → `round_level + linear_level − 1`（≤IV）。
- **深孔**（黑度>母材）直判 IV。

### 6.3 可解释判定轨迹（审计级）【专业新增】
每次判定输出**完整溯源**：命中条款号、所用数值表、各缺陷计算值、合并逻辑、最终级别，供复核与争议追溯。支持"人机协同"覆盖并留存操作人/时间。

### 6.4 边界与不确定性触发
检测 uncertainty 高或缺陷尺寸临界（如长径≈T/2、点数临界）时，`need_review=True` 推送专业人员，结果回流（见 §5.5）。

### 6.5 接口
- `POST /api/judge` → `{joint_level, per_defect, basis:[条款], need_review, standard_id, standard_version}`

---

## 7. 模块 5 — 报告、数据与系统

### 7.1 数据模型（SQLAlchemy，可云迁移）
```sql
images(id, path, source_type, modality, pixel_spacing_mm, base_metal_thickness,
       iqi_pass, density, created_at, preprocess_params JSON)
defects(id, image_id, class_id, shape, L_mm, W_mm, area_mm2, perimeter_mm,
        position_x, position_y, confidence, uncertainty, joint_level,
        need_review, reviewed_by, standard_id, standard_version)
reports(id, image_id, joint_level, generated_at, pdf_path, standard_ref, signer)
models(id, name, version, metric_map JSON, created_at, note)   -- 模型注册
```
- v1 SQLite；v3 同 schema 迁移 PostgreSQL（SQLAlchemy 屏蔽差异）。

### 7.2 报告生成
- 模板化行业报告：封面/工件信息/检测参数/IQI 与黑度校验结论/**缺陷图谱**（裁剪+标注+当量尺寸）/**判定依据条款**/结论。
- 输出 **PDF/A** 长期归档；支持**数字签名**（可选）满足可追溯。

### 7.3 检索与统计
- 多条件检索（日期/级别/类型/工件号）、缺陷统计与趋势图；千级记录响应 < 1s。

### 7.4 MLOps（工程质量，抗过时）【专业新增】
- **数据版本**：DVC 管理数据集与标注版本。
- **实验追踪**：MLflow（可本地）记录超参/指标/产物。
- **模型注册**：`models` 表 + 模型卡（精度、数据分布、局限、 ethically 使用说明）。
- **评估 Harness**：固定测试集 + 持续评估脚本，PR/CI 中跑回归。
- **漂移监控**：定期用新数据评估，触发再训练告警。

### 7.5 安全与隐私（工业数据合规）【专业新增】
- 本地优先、全离线；数据库文件**静态加密**（AES）；访问需本地口令/系统账户。
- 图像不出本机（除非用户显式开启云同步）；审计日志防篡改。

### 7.6 端边云预留（SyncAdapter）
```python
class Syncer(Protocol):
    def push(self, record) -> None: ...
    def pull(self) -> list: ...
    def federate(self, weights) -> None: ...   # v3 联邦平均
```
v1 = `LocalAdapter`（空操作/本地）；v3 = `CloudAdapter`。

---

## 8. 数据契约（节选，扩展）
```json
{
  "image_id": "img_001",
  "modality": "DR", "pixel_spacing_mm": 0.0625,
  "iqi": {"type":"wire","achieved_wire":"W12","required":"W10","pass":true},
  "defects": [
    {"id":"d1","class_id":4,"class_name":"crack","shape":"linear",
     "L_mm":3.4,"W_mm":0.8,"area_mm2":2.1,"perimeter_mm":8.3,
     "confidence":0.92,"uncertainty":0.18,"position":{"x_mm":12.4,"y_mm":5.1}}
  ],
  "judgment": {"standard_id":"NB/T47013.2-2015","standard_version":"2015",
               "joint_level":"II","basis":["6.1.6 条形缺陷","表X"],
               "need_review":false}
}
```

---

## 9. 目录结构（升级）
```
scan-detection/
├─ src/                      # Tauri + Vue3 前端
├─ src-tauri/                # sidecar 声明、窗口、安全
├─ backend/
│  ├─ app/                   # 应用层（用例编排）
│  ├─ domain/                # 领域接口 + 默认实现
│  │  ├─ interfaces.py       # Protocol: Detector/Grader/Preprocessor...
│  │  ├─ preprocess/  detect/  quantify/  grade/  report/  iqi/
│  │  └─ standards/          # 各标准适配模块 + tables/*.yaml
│  ├─ infra/                 # ModelStore / DB(SQLAlchemy) / Crypto / Log
│  ├─ models/                # 训练(train.py) + 权重 + 模型卡
│  └─ tests/                 # 单测 + 评估 harness
├─ data/                     # DVC 追踪（raw/labeled/synthetic）
├─ docs/  configs/  migrations/
```

---

## 10. 里程碑与演进路线图（至 2029）
- **M1** 骨架：Tauri+Vue + Python sidecar + REST/gRPC + SQLite + 接口契约。
- **M2** 接入与校验：DICOM 接入 + IQI 自动灵敏度校验 + 黑度。
- **M3** 预处理：§4.3 算法 + 质量度量门禁。
- **M4** 检测量化：模型无关检测器 + SAM 精修 + 量化 + 不确定性。
- **M5** 标准判定：多标准适配框架 + NB/T47013 实现 + 可解释轨迹 + 人工复核。
- **M6** 报告与 MLOps：PDF/A + 检索统计 + DVC/MLflow/评估 harness。
- **M7（演进）** 主动学习闭环上线，模型持续迭代。
- **v2/v3** 边缘集群、云端 + 联邦学习（按 §3.2 接口扩展）。

---

## 11. 风险与对策（更新）
| 风险 | 对策 |
|---|---|
| 标准数值非权威 | 结构化表占位 + `standard_version`，拿到授权原文即替换；多标准适配隔离变更 |
| 模型范式过时 | 模型无关接口；ONNX 解耦；默认实现可随 SOTA 替换 |
| 未标注数据多 | 主动学习 + 半监督 + 物理仿真增广 |
| 细缺陷漏检 | BiFPN 多尺度 + SAM 精修 + 难例挖掘 |
| 跨设备/黑度漂移 | DICOM 标定 + IQI 校验 + 漂移监控告警 |
| 工业数据隐私 | 本地优先 + 加密 + 联邦学习（数据不出行） |
| 算力限制 | ONNX Runtime/TensorRT 加速；nano 模型默认；heavy 路径可选 |

---

## 12. 补充功能（基于 NDT 领域实践的增强，非冗余）

> 设计者在审阅原始需求与工业射线评片真实流程后，补充以下五项。入选标准：① 直接回应用户已声明的"现场大批量图像快速筛查"；② 符合 NB/T47013 评片的实际行业规程；③ 强化"人机协同 / 合规追溯"主线，不与现有模块重叠或堆砌。

### 12.1 批量处理与任务队列
- **动机**：用户明确要"大批量图像快速筛查"，单张模式不满足现场产能。
- 能力：多底片导入（文件夹/设备）、任务队列、多 worker 并行推理、进度可视化、断点续跑、失败隔离。
- 接口：`POST /api/batch`（提交）、`GET /api/batch/{id}`（进度/结果）、`POST /api/batch/{id}/cancel`。

### 12.2 双人评片一致性工作流
- **动机**：真实射线评片规程要求两名评片员独立评定，分歧超阈值交第三名仲裁。这是合规可信度的行业硬要求，而非噱头。
- 能力：内置"初评 → 复评 → 仲裁"状态机；记录双人结论与一致性（Cohen's κ）；分歧自动升级人工；把"单人自动判定"纳入受控双人复核框架。
- 不替代标准判定逻辑，仅在其外层加合规壳。

### 12.3 可解释性热力图辅助复核
- **动机**：疑难/低置信缺陷推送人工时，附类激活热力图（Grad-CAM / 注意力）可让评片员秒懂模型关注区，降低复核成本。
- 能力：对 `uncertainty` 高或临界缺陷生成热力图叠加原图；**默认关闭**，仅人工复核视图开启，不进入主推理链路（避免拖累速度）。

### 12.4 设备标定向导与标定档案
- **动机**：直接支撑 NFR 中的"跨设备/跨工况一致率下降 ≤5%"，并解决黑度/像素标定录入的易错问题。
- 能力：首次接入设备引导录入像素间距、黑度基准、IQI 类型，存为"设备标定档案"；后续底片自动套用，减少机位漂移。

### 12.5 不可变审计日志
- **动机**：认证机构追溯要求——谁、何时、对哪张底片做过评片/复核/覆盖/模型切换，且不可篡改。
- 能力：append-only 审计日志（操作人、时间、对象、前后值、哈希链）；与 §7.5 安全加密协同。不属于业务功能，是合规底座。

---

## 13. 编程规范（工程基线，工程实现必须遵守）

### 13.1 语言与工具基线
- Python 3.12（兼容 3.13）；**类型标注强制**（PEP 484/585/604），CI 跑 `pyright --strict`（或 mypy strict）。
- TypeScript 5 `strict: true`；Vue 3 `<script setup lang="ts">`。
- 格式化：Python `black` + `ruff`（lint）；前端 `prettier` + `eslint` + `vue-tsc`。
- 包管理：Python `uv`/`poetry`（lockfile 必提交）；前端 `pnpm`。
- 提交：Conventional Commits（`feat:`/`fix:`/`refactor:`/`docs:`/`test:`/`chore:`）；trunk-based + 短生命周期分支；PR 必过 CI。

### 13.2 目录与命名
- 分层明确：`app/`(应用编排) `domain/`(领域，禁 IO) `infra/`(基础设施，禁业务)。依赖方向只能外层→内层（依赖倒置）。
- Python：`snake_case` 函数/变量，`PascalCase` 类/异常，`UPPER_CASE` 常量；接口 `XxxProtocol` 或 `AbstractXxx`，实现 `ConcreteXxx` 或具体名。
- TS：`camelCase` 变量/函数，`PascalCase` 组件/类型，`kebab-case` 文件名。

### 13.3 类型与接口契约（核心）
- 领域接口一律 `typing.Protocol` + docstring（职责 + 不变量）。
- 跨边界 DTO：内部 `@dataclass(frozen=True)`，API 用 **Pydantic**；**禁止裸 dict 跨层**。
- 枚举固化：`DefectClass` / `DefectShape` / `JointLevel` / `StandardId` / `Modality`。
```python
from enum import Enum
from typing import Protocol, Optional
from dataclasses import dataclass

class DefectClass(Enum):
    POROSITY=0; SLAG=1; INCOMPLETE_PEN=2; LACK_FUSION=3; CRACK=4

@dataclass(frozen=True)
class Detection:
    bbox: tuple[float, float, float, float]
    class_id: DefectClass
    score: float
    uncertainty: float
    mask: Optional["np.ndarray"] = None

class DefectDetector(Protocol):
    def load(self, model_uri: str, backend: str = "onnx") -> None: ...
    def infer(self, image: "np.ndarray", conf: float, iou: float) -> list[Detection]: ...
```

### 13.4 错误处理
- 自定义异常族：`domain/errors.py`（`ImageQualityError`/`ModelLoadError`/`GradingError`…），继承 `AppError`（带 `code` + `http_status`）。
- 禁止 `except:` 裸捕获；捕获后必须 log + 按语义处理或重抛。
- API 统一错误包 `{"error":{"code":str,"message":str,"detail":Any}}`，全局异常处理器映射 HTTP 状态。

### 13.5 日志与可观测
- 结构化 JSON 日志；每请求 `correlation_id`；分级；**禁止记录密钥、图像像素、个人敏感字段**。
- `/api/health` + metrics（时延、队列长度、模型版本）；前端错误统一上报。

### 13.6 配置管理
- 全部可调参数入 `configs/*.yaml`（pydantic-settings / OmegaConf），环境变量可覆盖；**禁止硬编码端口/路径/密钥**。
- 配置 schema 版本化；缺省值即安全默认值（本地绑定、加密开）。

### 13.7 测试规范
- 单测（pytest）：领域逻辑覆盖，IO 用 fake/mock；集成（httpx TestClient）验 API 契约；**评估 harness** 固定测试集跑 mAP/一致率，PR 设回归门禁（不得降超阈值）。
- 覆盖率：domain ≥ 80%；CI 顺序 `lint → type-check → test → eval → build`。
- 测试数据独立目录，绝不连生产库。

### 13.8 数据与模型版本
- DVC 追踪 `data/`；模型语义版本 `MAJOR.MINOR.PATCH`（不兼容/新标准/修补）；权重存 ModelStore 带 sha256；**模型卡必填**（精度、数据分布、已知局限、适用边界）。
- 数据集卡记录来源、标注规范、类别分布、许可。

### 13.9 安全编码
- 外部输入经 Pydantic 校验；文件类型/大小白名单；防路径遍历（`os.path.realpath` 校验在基目录内）。
- 临时文件用 `tempfile` 安全目录；敏感配置走环境变量/密钥管理，不入库不提交。
- 数据库静态加密（AES-256）；审计日志 append-only + 哈希链防篡改。

### 13.10 API 设计
- 资源式、版本化 `/api/v1/...`；列表分页 + 过滤；幂等（同输入同结果）；OpenAPI 随构建自动产出。
- 入参/出参严格 Pydantic schema；breaking change 升 `/v2`。

### 13.11 性能与并发
- FastAPI 全异步；重推理入进程池/独立 worker，不阻塞事件循环；批量接口并行；模型常驻并支持版本热切换。

### 13.12 文档
- 公共函数 Google 风格 docstring（Args/Returns/Raises）；模块 docstring 说明职责；架构决策记 ADR（`docs/adr/`）；目录 README。
- 本规格书、接口契约、数据契约入版本库，作为实现唯一事实源。

### 13.13 发布与打包
- 可复现构建（lockfile + 环境冻结）；依赖 Pin；Tauri sidecar 签名；应用语义版本；`CHANGELOG.md`。

---

---

## 14. 统一 API 契约（v1，集中参考）

> 此前 §4–§7 提到的端点在此统一为 `/api/v1/...`（短名 `/api/...` 保留为兼容别名）。所有请求/响应强制 Pydantic 校验；错误统一包 `{error:{code,message,detail}}`，由全局处理器映射 HTTP 状态。

| 方法 | 路径 | 用途 | 关键入参 | 关键出参 |
|---|---|---|---|---|
| GET | `/api/v1/health` | 存活/版本/模型状态 | — | `{status, app_version, model_version, gpu}` |
| POST | `/api/v1/verify` | IQI+黑度校验 | `image`, `modality`, `iqi_type?` | `{iqi:{type,achieved,required,pass}, density, evaluable}` |
| POST | `/api/v1/preprocess` | 预处理 | `image`(multipart), `params?` | `{enhanced_image_id, thumbnail, metrics:{brisque,psnr?,ssim?}}` |
| POST | `/api/v1/detect` | 检测+量化 | `image`, `pixel_spacing_mm?`, `conf`, `iou`, `explain?` | `{defects:[Detection], annotated_image}` |
| POST | `/api/v1/judge` | 标准判定 | `defects`, `base_metal_thickness_T`, `standard_id?` | `{joint_level, per_defect_grade[], basis[], need_review, standard_id, standard_version}` |
| POST | `/api/v1/batch` | 批量提交 | `images[]`, `options` | `{batch_id, estimated}` |
| GET | `/api/v1/batch/{id}` | 批量进度 | — | `{status, progress, results[]}` |
| POST | `/api/v1/batch/{id}/cancel` | 取消批量 | — | `{ok}` |
| POST | `/api/v1/review` | 双人复核/仲裁 | `image_id`, `reader_role`, `grades`, `overrides?` | `{consensus, kappa, needs_arbitration}` |
| POST | `/api/v1/explain` | Grad-CAM 热力图 | `image`, `defect_id` | `{heatmap}` |
| POST | `/api/v1/report` | 生成报告 | `image_id`, `template?` | `{pdf_url}` / 二进制 |
| GET | `/api/v1/records` | 检索/统计 | `level,class,from,to,page,size` | `{items[], total, stats}` |
| GET | `/api/v1/models` | 模型列表 | — | `{models:[{id,version,metric_map}]}` |
| POST | `/api/v1/models/{id}/activate` | 模型热切换 | — | `{ok, active}` |
| GET | `/api/v1/audit` | 审计日志 | 过滤条件 | `{entries[]}` |

**Detection 响应 schema（权威定义）**
```json
{ "id":"d1","class_id":4,"class_name":"crack","shape":"linear",
  "bbox":[x,y,w,h],"confidence":0.92,"uncertainty":0.18,
  "L_mm":3.4,"W_mm":0.8,"area_mm2":2.1,"perimeter_mm":8.3,
  "position":{"x_mm":12.4,"y_mm":5.1},"mask_ref":"<uri|null>" }
```
**错误码**：`IMG_UNREADABLE(400)`、`IQI_FAIL(409)`、`MODEL_UNAVAILABLE(503)`、`GRADING_AMBIGUOUS(422)`、`UNKNOWN(500)`。

---

## 15. 评测与度量规范（定义"好"的硬性标准）

### 15.1 检测
- **主指标**：mAP@0.5、mAP@0.5:0.95；**每类 AP**（5 类分别报）；小目标 AP（缺陷常 <32px）。
- 辅助：Precision/Recall/F1、混淆矩阵（5 类 + 背景）、漏检率（尤其裂纹）。
- 目标：mAP@0.5 ≥ 0.90；裂纹类 AP ≥ 0.85（最难）。

### 15.2 量化
- 相对误差：|测量−人工|/人工，对 L/W/面积；目标 ≤ 5%。
- 一致性：Bland–Altman 分析（偏差≈0、95% 限内）。

### 15.3 判定（与高级评片员比对）
- 总体一致率、各级别一致率、**Cohen's κ**（≥0.8 视为高度一致）；分歧率。
- 边界缺陷（长径≈T/2、点数临界）专项准确率。

### 15.4 校准与不确定性
- 置信度校准：**ECE（期望校准误差）** ≤ 0.05；不确定性（MC Dropout，T=10）熵阈值触发复核。

### 15.5 鲁棒性
- 跨设备/跨工况一致率下降 ≤ 5%；噪声/对比度压力测试不崩。

### 15.6 回归门禁（CI 强制）
- PR 不得使 mAP@0.5 下降 >1.0 点、一致率下降 >2%、ECE 恶化 >0.02；否则阻断合并。
- **Golden Set**：固定、版本化、禁止用于训练；每次提交自动评估。

---

## 16. 边界场景与验收用例（证明鲁棒性）

| # | 场景 | 期望行为 |
|---|---|---|
| 1 | 非焊缝/空区域 | 不产生假缺陷 |
| 2 | 黑度过低/过高 | 密度门禁触发，提示重拍或增强后仍不达则阻断 |
| 3 | 双重曝光/叠片 | 检测异常并告警，不误判 |
| 4 | 密集气孔群 | 逐个数出并作圆形缺陷分组评级 |
| 5 | <1mm 微裂纹 | SAM 精修 + 高 uncertainty → 推送人工 |
| 6 | 缺陷贴边 | 边界框/轮廓正确处理，不截断误算 |
| 7 | IQI 缺失/不可读 | verify 失败，阻断评片 |
| 8 | 切换标准（如选 ASME） | Grader 切换且记录 standard_version |
| 9 | 全图低置信 | 整体升级人工，不强行给级 |
| 10 | 千张级批量 | 队列推进、进度可见、中断可续跑、单张失败隔离 |

---

## 17. 训练流水线规格

- **框架**：Ultralytics YOLO；骨干 nano（v8/v11）；输入 640×640；导出 ONNX(opset17)→可选 TensorRT。
- **优化**：AdamW / SGD + 余弦退火；epochs≈300；batch 16–32；损失默认 + 对裂纹类 **Focal** 抑不平衡。
- **增强**（Albumentations）：旋转、亮度/对比度抖动、加噪、Mosaic、MixUp；按类均衡采样（裂纹过采样/类权重）。
- **未标注利用**：conf>0.9 伪标签 → 入训练池再训；主动学习挑高价值样本优先人工标。
- **不确定性**：推理期 MC Dropout（T=10）估熵。
- **数据治理**：8:1:1 按类+设备分层抽样；DVC 版本化；训练/验证/测试集固定种子、Golden Set 隔离。
- **产出**：`best.pt`/`onnx` + 模型卡（指标、数据分布、局限、适用边界）。

---

## 18. 术语表与需求追溯（轻量）

**术语**：RT=射线检测；IQI=像质计（灵敏度校验）；CR/DR=计算机/数字射线成像；DICOM=医学/工业影像标准；mAP=平均精度均值；κ=Cohen 一致性系数；ECE=期望校准误差；SAM=Segment Anything 实例分割。

**需求→模块追溯（摘要）**
| 需求 | 落实模块 | 验证 |
|---|---|---|
| 降噪/对比度/边缘 | §4 预处理 + IQI | §15 质量度量 / §16 场景2 |
| 5 类缺陷定位量化 | §5 检测+量化 | §15.1/15.2 / §16 场景4,5 |
| NB/T47013 判定 | §6 标准适配 | §15.3 / §16 场景7,8 |
| 报告/检索 | §7 报告+数据 | §14 records/report |
| 批量筛查 | §12.1 队列 | §16 场景10 |
| 合规追溯 | §12.5 审计 + §7.5 | §14 audit |
| 抗过时/可插拔 | §3 架构 + §13 规范 | 接口契约稳定 |

---

---

## 19. 协作治理与架构护栏（防技术债务的核心）

> 本规格书被多轮增量开发时，最大风险是"各任务孤立完成 → 接口漂移 → 层间越权 → 债务积累"。本章把所有架构约束**变成可机械校验的规则**，使任何一轮开发迭代都必须在护栏内施工。

### 19.1 分层依赖铁律（必须被工具强制）
依赖方向严格单向：**表现层 → 应用层 → 领域层 → 基础设施层**。规则：
- 领域层（`domain/`）**禁止** import 业务无关的 I/O、`fastapi`、以及 `infra/`；领域只定义接口与纯逻辑/算法（调用 `cv2`/`numpy` 属算法非 I/O，允许）。
- 基础设施层（`infra/`）**禁止**包含业务判定逻辑；只提供能力（存储/模型加载/加密/文件）。
- 应用层（`app/`）只做编排，不写算法；通过依赖注入装配领域接口的实现。
- 跨层数据用 DTO（`domain/dto.py`），**禁止**直接传 ORM 对象或框架请求对象跨层。
- **强制工具**：`import-linter` 定义 `layers: presentation, application, domain, infrastructure`，配置 `forbidden` 规则（如 `infrastructure` 不得依赖 `domain` 业务符号、任何层不得反向依赖上层）；CI 中 `lint-imports` 失败即阻断。

### 19.2 权威文件树与每文件职责
```
scan-detection/
├─ src/                         # 表现层(Vue3/TS)：仅 UI 与调用 /api/v1
├─ src-tauri/                   # Tauri 配置：sidecar 声明、窗口、token
├─ backend/
│  ├─ app/                      # 应用层：API 路由 + 用例编排（无算法）
│  │   ├─ routers/              # 每资源一 router（对应 §14）
│  │   └─ pipelines.py          # 校验→预处理→检测→判定→报告 编排
│  ├─ domain/                   # 领域层：接口 + 纯逻辑（禁业务 I/O）
│  │   ├─ interfaces.py         # 所有 Protocol（Detector/Grader/Preprocessor/IQIVerifier/Quantifier/Reporter/Syncer）
│  │   ├─ dto.py                # 跨层 DTO（dataclass/frozen）
│  │   ├─ errors.py             # AppError 异常族
│  │   ├─ preprocess/          # 预处理纯逻辑
│  │   ├─ detect/              # 检测编排（模型加载由 infra 提供）
│  │   ├─ quantify.py          # 量化（轮廓/标定）
│  │   ├─ grade/               # 标准判定纯逻辑
│  │   └─ standards/           # 每标准一模块 + tables/*.yaml（数值表）
│  ├─ infra/                    # 基础设施层：能力实现（禁业务）
│  │   ├─ model_store.py       # 模型加载/版本/哈希（实现 DefectDetector 后端）
│  │   ├─ db.py                # SQLAlchemy 引擎/会话（实现存储接口）
│  │   ├─ crypto.py            # AES 加密
│  │   └─ fs.py                # 安全文件/临时目录
│  ├─ models/                   # 训练 train.py + 权重 + 模型卡
│  ├─ configs/                  # *.yaml（禁硬编码）
│  └─ tests/                    # 单测/集成/契约测试/评估 harness
├─ data/                        # DVC 追踪（raw/labeled/synthetic/golden）
├─ docs/adr/                    # 架构决策记录
└─ migrations/                  # 数据库迁移（Alembic）
```
任何新代码**必须**落入上述目录；禁止在根目录或随意位置新建 `.py`；新增能力先扩接口，再在实现层落地。

### 19.3 接口契约唯一真源
所有领域接口集中在 `domain/interfaces.py`，签名即合同。AI 实现任一能力时**只能满足接口，不得新增必需参数破坏调用方**。当前契约摘要：
- `DefectDetector.load(model_uri, backend) / infer(image, conf, iou) -> list[Detection]`
- `StandardGrader.grade(defects, context) -> GradeResult`（带 `standard_id`/`standard_version`）
- `Preprocessor.denoise / enhance / edges / morph`
- `IQIVerifier.verify(image) -> IQIResult`
- `Quantifier.measure(detection, pixel_spacing_mm) -> Geometry`
- `Reporter.build(image_id, template) -> pdf`
- `Syncer.push / pull / federate`（v1 = `LocalAdapter`）
新增能力同理：先扩 `interfaces.py` 并经 ADR 批准，再实现。

### 19.4 扩展菜谱（防"另起炉灶"）
- **新增标准**（如 ASME）：复制 `standards/nb47013.py` → `asme.py`，实现 `StandardGrader`，加 `tables/*.yaml`，注册到标准目录；**不改**判定主逻辑与数据模型（`standard_id`/`version` 已预留）。
- **替换检测器**（如换 RT-DETR）：在 `infra/model_store.py` 新增 `RTDETRDetector` 实现 `DefectDetector`，经配置切换；**不改** `detect/` 编排与 API。
- **新增缺陷类别**：`DefectClass` 枚举加值 → 标注 → 重训 → DTO 自然容纳；前端从枚举读取，无需硬改。
- 以上均"接口不动、实现可插拔"，是抗债务的根本。

### 19.5 增量任务模板（每轮 AI 任务的统一结构）
每个编程任务必须包含以下字段，缺一项视为需求不清、不予开工：
```
【模块】app / domain / infra 哪一层
【接口】实现/修改哪个 Protocol 或 router（引用 §19.3 签名）
【入参/出参】严格 Pydantic/DTO schema（不得偏离 §14 / §19.3）
【约束】禁跨层调用、禁硬编码、类型标注、docstring、单测
【验收】对应 §15 指标 / §16 场景 / 契约测试
【禁止】不新增顶层目录、不改他模块内部、不引入未声明依赖
```
**Definition of Done（合并前必过）**：① pyright/ruff/eslint 绿；② import-linter 绿；③ 单测+契约测试绿；④ 评估 harness 未触发回归门禁；⑤ 文档/ADR 同步。

### 19.6 契约测试与 CI 硬门禁
- **契约测试**：对每个 `Protocol` 实现写 conformance test（签名/异常/边界），实现替换即自动校验。
- **CI 流水线**（顺序，任一失败阻断）：`ruff+black → pyright --strict → eslint+vue-tsc → import-linter → pytest(单测+契约) → eval-harness(回归门禁) → build`。
- **预提交**：pre-commit 跑格式化与 lint，杜绝风格漂移。

### 19.7 版本与兼容策略
- API：`/api/v1` 起；破坏性变更升 `/v2`，旧版保留至无调用方。
- 模型：`MAJOR.MINOR.PATCH`；旧权重并存、运行时热切换；评估对比写入模型卡。
- 标准：`standard_version` 随记录落库；新版本并行支持，历史数据可比。
- 应用：语义版本 + `CHANGELOG`；Alembic 迁移保证 DB 可升级可回滚。

### 19.8 ADR 索引（关键决策固化，防反复）
- ADR-001 技术栈 Tauri+Vue3+Python sidecar
- ADR-002 检测用模型无关接口（默认 YOLO）
- ADR-003 标准用适配层 + 版本化
- ADR-004 本地优先/离线/加密（数据不出行）
- ADR-005 分层依赖铁律 + import-linter 强制
- ADR-006 评估回归门禁 + Golden Set 禁训练
（每个 ADR 记 `docs/adr/ADR-XXX.md`，含背景/决策/后果/备选；新决策须先写 ADR 再动手）

### 19.9 AI 生成代码合并检查清单
- [ ] 仅改动任务声明的模块/文件
- [ ] 未新增顶层目录或绕过接口
- [ ] 类型标注完整、docstring 齐全
- [ ] 无硬编码（端口/路径/密钥/数值表）
- [ ] 新增/修改处有单测与契约测试
- [ ] 评估未触发回归门禁
- [ ] 文档（接口/ADR/数据契约）已同步

---

*本规格书为工程级、抗过时、可机械护栏的 迭代开发契约：接口契约稳定、实现可插拔、标准可版本化、数据可闭环进化、评测有硬性门禁、边界有验收用例、分层有 import-linter 强制、增量有任务模板与合并清单。补充功能均源于 NDT 领域规程与用户已声明的批量筛查需求，非冗余堆叠。模块 4 标准数值待授权原文固化。*
