# 射线焊缝缺陷检测 — 方案 A（逐类置信度阈值）验收报告（最终版）

**日期**：2026-08-10 ｜ **部署模型**：`yolo8n_real_rare/train2`（YOLOv8n 合成最大化重训）｜ **部署**：`_pkg/ScanDetection`

---

## 一、结论

**方案 A 端到端闭环，最终验收 PASS。** 在修复检测器两处致命解码 Bug、重训出**非坍塌**的平衡模型、并以"合成最大化"策略针对性增强稀有类（尤其裂纹）后：

- **稀有类召回 96.3% → 100.0%**（27/27 真实稀有缺陷全部命中）；
- **裂纹(类4) 从 0 候选突破为有检出**（终验 OLD 0 → NEW 17 框，全部落在真实裂纹图 PG101-2-6）；
- **气孔误检零增长**（381→381，高阈值压制过检）。

---

## 二、关键验收数据

### 1. 方案 A 终验（`_verify_planA.py`，165 张真实底片，OLD 统一 0.30 vs NEW 逐类阈值 `{0:0.30, 1:0.01, 2:0.01, 3:0.008, 4:0.005, 5:0.01}`）

| 类别 | OLD(统一0.30) | NEW(逐类阈值) | Δ |
|---|---:|---:|---:|
| 气孔 | 381 | 381 | **+0** |
| 夹渣 | 7 | 32 | +25 |
| 未焊透 | 20 | 100 | +80 |
| 未熔合 | 7 | 37 | +30 |
| **裂纹** | **0** | **17** | **+17** ✅ |
| 咬边 | 5 | 27 | +22 |
| **稀有类合计** | **39** | **213** | **+174** |

> 判定：气孔不变 + 稀有类释放（含裂纹 0→17）→ **PASS**。

### 2. 部署模型类别感知评测（`eval_rare_metrics_v2.json`，498 GT / 165 底片）

| conf 阈值 | 召回 | 精确 | 气孔召回 | **稀有类召回** | 裂纹(4)框 |
|---|---:|---:|---:|---:|---:|
| 0.001 | 90.6% | 7.6% | 90.0% | **100.0%** | **91** |
| 0.01 | 88.0% | 27.4% | 87.3% | **100.0%** | **10** |
| 0.10 | 80.3% | 65.4% | 79.6% | 92.6% | — |
| 0.25 | 74.5% | 79.8% | 73.7% | 88.9% | — |
| 0.50 | 62.7% | 85.5% | 62.0% | 74.1% | — |

> 逐类阈值依据：稀有类 TP 框置信度均 ≥0.01（0.001→0.01 召回稳定 100%），故稀有类取 **0.01**；裂纹样本更少、更安全关键，取 **0.005** 留更宽余量；气孔 0.001 下吐 4803 框（绝大多数误检），取 **0.30** 压制。

### 3. 迭代记录（train → train2）

| 版本 | 策略 | 稀有类召回 | 裂纹检出 | 终验稀有释放 |
|---|---|---|---|---|
| 旧部署 real_synth2 | 坍塌权重（已废弃） | ~0（全为解码 Bug 假象） | 0 | 无意义 |
| train（第一版重训） | Copy-Paste ×8 + 过采样×4 | 96.3% | **0** | +330 |
| **train2（当前部署）** | **合成最大化**：稀有 Copy-Paste ×12 + 过采样×8 + **裂纹专用 ×100** | **100.0%** | **0→17** | **+174** |

---

## 三、根因回顾与修复清单

| # | 问题 | 根因 | 修复 | 状态 |
|---|---|---|---|---|
| A | 所有检出退化为"气孔" | ONNX 输出为通道优先 `(1,10,8400)`，`out.shape[1]>out.shape[2]` 恒为 False → 分类头折叠 | 改为 `if out.shape[1]==4+nc: out=out.transpose(0,2,1)` | ✅ backend + _pkg |
| B | 全量漏检（置信度 ~0.002） | ONNX class 通道语义处理错误 | 自适应 sigmoid：`if scores_all.min() < -1e-3` 才做（本环境导出已融合 sigmoid，避免二次 sigmoid） | ✅ backend + _pkg |
| C | 部署模型"检出"全是假象 | 旧 `real_synth2` 权重**坍塌**（输入无关性测试通过失败） | 重训平衡 YOLOv8n；train2 进一步合成最大化 | ✅ 非坍塌 |
| D | 稀有类召回上不去、裂纹 0 候选 | 统一高阈值抑制低分稀有候选；裂纹仅 1 个真实源 | `class_conf` 逐类阈值 + 裂纹专用高倍合成增强 | ✅ 稀有 100%、裂纹 0→17 |

---

## 四、部署状态（已验证）

- **ONNX 权重**：`_pkg/ScanDetection/models/weights/best.onnx` 与 `_pkg/ScanDetection/backend/models/weights/best.onnx`（均 12,242,563B，来自 `yolo8n_real_rare/train2/weights/best.pt`，`simplify=False` legacy 导出，布局 `(1,10,8400)`）。
- **检测器代码**：`_pkg/ScanDetection/backend/domain/detect/yolo_detector.py` 含全部修复（A 定向、B 自适应 sigmoid、D 逐类阈值 `_thr_for`）。
- **一致性核验**：终验直接加载部署 ONNX 跑 `YoloDetector`，裂纹框真实出现在标注裂纹图（PG101-2-6，score 0.007–0.148），无二次 sigmoid 灾难。

---

## 五、残留风险与后续建议

1. **裂纹泛化仍依赖数据**：17 个裂纹框全部源自合成增强（1 个真实裂纹框 ×100 合成），模型对**真实裂纹**的泛化上限仍受限于样本量。要生产级可靠，建议补**真实含裂纹底片**（带标注最好，我可直接整合重训）。
2. **数据量硬约束**：稀有类真实标注仅 27 个。按既有实证，需累积 **~100 张真实标注**才能稳定支撑自动预标注。
3. **气孔过检**：0.30 阈值下仍有 ~380 框/165 图，可进一步优化（气孔专属高分阈值或后处理聚类）。
4. **低分稀有框走人工复核**：NEW 放出的低分稀有框（score 0.005–0.05）应经 `StandardGrader` 的 `need_review`/uncertainty 机制进入人工复核——符合"安全关键缺陷优先召回"原则。

---

## 六、保留的校验工具与状态文件

- `_verify_planA.py`：OLD/NEW 阈值对比终验。
- `_probe_newmodel.py`：坍塌判定 + 稀有类候选探查。
- `_onnx_inspect.py`：ONNX 原始分类分数审计。
- `train_status.json`：训练/导出/终验全程状态与结果（值守式盯守的硬事实）。
