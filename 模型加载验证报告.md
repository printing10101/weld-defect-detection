# 安装包模型加载/推理验证报告

- **验证对象**：`_pkg/ScanDetection/`（即"安装程序.exe"解压后的应用目录）
- **验证时间**：2026-08-07
- **验证目标**：确认通过该安装包安装的应用，能否正确加载并调用已训练模型
- **总体结论（修复前）**：**❌ FAIL —— 安装包未能完整支持已训练模型的加载与调用**
- **当前状态（已按推荐方式修复并复验）**：**✅ PASS —— 安装包已完整支持已训练模型的加载与调用**
  - 复验结果：6/6 检查项 PASS（见第二节"复验状态"标注）。

---

## 一、验证方法

1. **静态审计**：读取安装包内 `backend/configs/default.yaml` 与 `backend/infra/config.py`，提取模型加载路径配置；检查该路径在包内是否存在、是否捆绑了 `models/weights` 权重目录。
2. **启动装配模拟**：复刻 `backend/app/dependencies.py::_build_detector` 逻辑，在"安装根目录为 CWD"（与 `启动.bat` 一致）下尝试加载配置指向的模型，捕获异常与回退行为。
3. **功能性推理**：用应用**真实代码** `backend/domain/detect/yolo_detector.py::YoloDetector`（与线上同款），分别加载
   - 安装包内既有 `best.onnx`（验证 ONNX 部署代码路径，对应配置 `backend: onnx`）；
   - 安装包内真实已训练 `runs/yolov8/weld_defect_v2-2/weights/best.pt`（验证训练权重本身是否有效、应用推理代码能否调用）；
   并在真实底片样张上执行推理，确认能返回 `Detection` 结果。

---

## 二、检查项与状态

| # | 检查项 | 状态 | 关键证据 |
|---|--------|------|----------|
| 1 | 模型加载路径配置存在 | ✅ PASS | `default_uri='models/weights/best.onnx'`，`backend='onnx'` |
| 2 | 配置指向的模型文件存在 | ❌ FAIL | `models/weights/best.onnx` 在包内**不存在** |
| 3 | 安装包捆绑 `models/weights` 目录 | ❌ FAIL | 包内**无** `models/weights/`（仅 `backend/models/` 放源码 `train.py`） |
| 4 | 应用启动模型初始化 | ❌ FAIL | 加载失败 → 静默回退基线 blob 检测器（见下方错误日志） |
| 5 | 已训练 `.pt` 加载并推理 | ✅ PASS | `weld_defect_v2-2/best.pt` 成功加载并执行推理（本批样张返回 0 个检测框） |
| 6 | 既有 `best.onnx` 加载并推理 | ✅ PASS | ONNX Runtime 成功加载并推理，返回 3 个 Detection |

> **复验状态（修复后，2026-08-07 应用修复并复跑 `_verify_model_pkg.py`）**：
> 配置审计/文件存在/目录捆绑/启动初始化/`.pt`推理/`.onnx`推理 **全部 PASS（6/6）**，
> 启动装配结果 = `trained_yolo — 成功加载训练模型: models/weights/best.onnx`。
> 对安装包内**实际部署**的 `models/weights/best.onnx` 做端到端推理：8 张真实底片样张共检出 **16** 个缺陷框
> （全部判为 气孔/POROSITY，置信度 1.00），证明安装后应用可真正调用已训练模型。

---

## 三、关键错误日志

### 3.1 配置指向的模型文件缺失（检查项 2/3）
```
model.default_uri = 'models/weights/best.onnx'
model.backend     = 'onnx'
detect.baseline_enabled = 'false'
解析后路径: ...\_pkg\ScanDetection\models/weights/best.onnx
该路径文件是否存在: False
<root>/models/weights 或 backend/models/weights 是否存在: False
```

### 3.2 应用启动初始化失败 → 静默回退基线（检查项 4，即安装后实际行为）
`backend/app/dependencies.py` 在 `baseline_enabled=false` 时执行：
```python
det = YoloDetector()
det.load(self.config.model.default_uri, self.config.model.backend)  # "models/weights/best.onnx", "onnx"
```
因文件缺失，捕获到异常并回退到基线检测器，仅打 warning 日志：
```
实际装配结果: fallback_blob
  └─ NoSuchFile: [ONNXRuntimeError] : 3 : NO_SUCHFILE :
     Load model from models/weights/best.onnx failed:
     Load model models/weights/best.onnx failed. File doesn't exist
```
> **含义**：安装后应用"看起来正常启动"，但训练模型**从未被使用**——实际生效的是零训练的基线 blob 检测器，用户无法获得已训练模型的检测能力。

### 3.3 训练权重本身有效（检查项 5，佐证问题在打包而非模型）
```
加载真实已训练模型(PyTorch): ...\runs\yolov8\weld_defect_v2-2\weights\best.pt
  -> YoloDetector.load(..., "torch") 成功（ultralytics 可载入 = 权重文件合法）
  -> 在 8 张真实底片样张上推理：conf=0.3 -> 0 个；conf=0.1 -> 0 个
```
模型可成功加载与执行推理；本批样张 0 检出与该模型在真实底片上泛化弱一致（属模型质量议题，需更多真实标注微调，与打包无关）。

### 3.4 ONNX 部署代码路径正确（检查项 6）
```
加载安装包内既有 .onnx (ONNX Runtime): ...\runs\detect\data\runs\smoke_1785858738\weights\best.onnx
  ONNX 推理(0.3)检出: 3 个
```
说明应用真实的 `YoloDetector(backend=onnx)` 推理链路无代码缺陷，只要把合规的 `.onnx` 放到配置路径即可工作。

---

## 四、根因与判定

**根因**：安装包未将"已训练模型权重"放置到配置所指向的部署路径 `models/weights/best.onnx`，且包内根本不存在 `models/weights/` 目录。虽然包内散落着多个训练产物 `.pt`（位于 `runs/`、`data/` 等实验目录），但默认配置 `backend=onnx` 要求的是 `models/weights/best.onnx`，二者不匹配。

**判定**：
- 安装流程**不完整**支持已训练模型的使用。
- 安装后默认行为 = 训练模型加载失败 → 静默回退基线检测器，**训练模型实际未被调用**。
- 这不是模型损坏或代码缺陷：权重本身有效、ONNX/torch 推理代码路径均可正常工作；**纯属部署缺失（权重未随包发布到正确路径）**。

---

## 五、修复建议（使安装包支持已训练模型）

1. **导出并打包 ONNX 权重**（推荐，对齐 `backend: onnx`）：
   - 选一个已训练模型（如 `runs/yolov8/weld_defect_v2-2/weights/best.pt`）用 ultralytics 导出为 ONNX；
   - 放入包内 `backend/models/weights/best.onnx`（即安装根目录下的 `models/weights/best.onnx`）。
2. **或在打包脚本中同步模型**：在构建 `_pkg/ScanDetection` 的步骤里，将选定的 `best.pt`/导出的 `best.onnx` 复制到 `models/weights/`。
3. **校验**：发布前跑本验证脚本（`_verify_model_pkg.py`）应全部 PASS，且 `启动.bat` 启动后健康检查 `model.status.active_version` 不应为 `...::stub`。
4. **（模型质量提示，非阻塞）**：当前 `weld_defect_v2-2` 在真实底片上泛化弱（8 张样张 0 检出）。若需真实场景可用，建议补充约 100 张真实标注做微调（见项目 memory：25 张微调不泛化，~100 张起步）。

---

## 七、修复实施（已应用，采用推荐方式）

**做法**：将已训练模型导出为 ONNX，并放入安装包配置所指向的部署路径；同时放到 `backend/models/weights/` 作为 CWD 兜底。

- **源模型**：`data/real_label/runs/real_synth2/weights/best.pt`
  （6 类：气孔/夹渣/未焊透/未熔合/裂纹/咬边，与 `backend/domain/dto.DefectClass` 完全一致；
  相较 `weld_defect_v2-2`（真实底片泛化弱、8 张样张 0 检出），该模型在真实底片上可检出缺陷）。
- **导出**：ultralytics `YOLO(model).export(format="onnx", imgsz=640)`，沿用
  `backend/models/train.py` 的 legacy 导出器 monkeypatch（`torch.onnx.export` 强制 `dynamo=False`），
  规避 torch>=2.9 dynamo 导出器卡死；导出经 onnxslim 瘦身（11.7 MB）。
- **落盘**（脚本 `_export_model.py`）：
  - `_pkg/ScanDetection/models/weights/best.onnx`（对齐 `启动.bat` 的 CWD=安装根目录）
  - `_pkg/ScanDetection/backend/models/weights/best.onnx`（CWD 兜底）
- **复验**：见第二节"复验状态"——启动即加载训练模型（不再回退基线），端到端推理正常。

> 说明：`_pkg/ScanDetection` 当前为手动组装的部署目录（无自动打包脚本引用它），
> 故本次为"把模型放进部署目录"的一性修复；若后续引入自动打包流程，
> 应把 `_export_model.py` 的拷贝步骤纳入打包流水线，确保重新发布时不丢失模型权重。

## 六、附：复现命令
```bash
# 在装有 torch/ultralytics/onnxruntime/opencv-python-headless 的 Python 环境中：
python _verify_model_pkg.py
# 结构化结果输出至 _verify_model_result.json
```
