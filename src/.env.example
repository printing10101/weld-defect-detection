# 前端构建/运行环境变量样例（复制为 .env 后修改）
# 所有变量以 VITE_ 前缀暴露给前端代码（Vite 约定）。

# 后端 API 基地址。
# 开发（vite dev 代理 /api 到 5173）：留空或 "/api/v1"
# 生产（Tauri 一体包 / 反向代理同源）："/api/v1"
# 独立部署后端在别的 host/port： "http://127.0.0.1:18773/api/v1"
VITE_API_BASE=/api/v1
